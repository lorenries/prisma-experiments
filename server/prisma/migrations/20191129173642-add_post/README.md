# Migration `20191129173642-add_post`

This migration has been generated by lorenries at 11/29/2019, 5:36:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lries"."Post" (
  "body" text NOT NULL DEFAULT '' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "link" text   ,
  "path" text NOT NULL DEFAULT '' ,
  "published" boolean NOT NULL DEFAULT false ,
  "publishedAt" timestamp(3)   ,
  "section" text NOT NULL DEFAULT '' ,
  "tags" text   ,
  "title" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lries"."User" (
  "email" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "name" text   ,
  "password" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "Post.path" ON "lries"."Post"("path")

CREATE UNIQUE INDEX "User.email" ON "lries"."User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191129173642-add_post
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,28 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://prisma:prisma@localhost:5432/prisma?schema=lries"
+}
+
+model Post {
+  id          String    @default(cuid()) @id
+  path        String    @unique
+  createdAt   DateTime  @default(now())
+  publishedAt DateTime?
+  published   Boolean   @default(false)
+  title       String
+  body        String
+  section     String
+  tags        String?
+  link        String?
+}
+
+model User {
+  id       String  @default(cuid()) @id
+  email    String  @unique
+  password String
+  name     String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191129173642-add_post)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191129173642-add_post'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
